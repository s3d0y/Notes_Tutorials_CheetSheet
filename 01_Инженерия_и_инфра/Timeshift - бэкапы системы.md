

```
# созадть снепшот
sudo timeshift --create --comments "First snapshot"

# Список снепшотов
sudo timeshift --list

# Размер первого снепшота
sudo du -sh /mnt/timeshift/timeshift/snapshots/*

# Проверьте что снепшот создался
ls -la /mnt/timeshift/timeshift/snapshots/
```

### **✅ ПОПАДАЕТ В СНЕПШОТ:**

```
/
├── /etc/          → ВСЕ конфиги системы
│   ├── ssh/       → настройки SSH (порт 3322, ключи)
│   ├── sudoers.d/ → права пользователей
│   ├── fail2ban/  → настройки защиты
│   ├── ufw/       → правила фаервола
│   └── network/   → сетевые настройки
├── /usr/          → установленные программы
├── /var/          → системные данные (кроме исключённого)
├── /boot/         → загрузчик и ядро
└── /root/         → домашняя папка root (исключена у вас)

```

```
/home/s3d0y/       → ваши файлы, скрипты, Docker-проекты
/var/lib/docker/   → контейнеры и образы Docker
/tmp/, /var/tmp/   → временные файлы
/var/cache/        → кэш пакетов
/var/log/          → логи системы
```


Восстановить

``` sh
sudo timeshift --restore
```
## 🚨 **ОТ ЧЕГО СПАСЁТ TIMESHIFT:**
### **1. Сломанные пакеты и обновления:**
### **2. Сломанные конфиги:**
### **3. Случайные изменения системных файлов:**
### **4. Проблемы с загрузчиком:**
### **5. Сетевые настройки:**


## 🛡️ **ЧТО НЕ СПАСЁТ (важно!):**

### **❌ Ваши данные в /home/s3d0y/:**
### **❌ Docker контейнеры и образы:**
### **❌ Базы данных внутри контейнеров:**
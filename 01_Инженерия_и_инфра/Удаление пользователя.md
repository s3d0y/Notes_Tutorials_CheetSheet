
``` sh
#!/bin/bash
echo "🔍 Проверяем пользователя hmsrvadm..."
sudo -i

echo "📋 Проверяем активность..."
ps -u hmsrvadm
who | grep hmsrvadm

echo "🗑️ Удаляем из sudoers..."
visudo -f /etc/sudoers.d/safe-admin
# Вручную удаляем hmsrvadm из User_Alias ADMINS

echo "👤 Удаляем пользователя..."
deluser --remove-home hmsrvadm

echo "✅ Проверяем удаление..."
getent passwd hmsrvadm && echo "❌ Ошибка: пользователь существует" || echo "✅ Пользователь удален"

echo "🔒 Проверяем безопасность..."
visudo -c
sudo -l | grep hmsrvadm && echo "❌ Остались права" || echo "✅ Права очищены"
```
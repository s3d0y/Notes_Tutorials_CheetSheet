
Для тренировки представим что нам необходимы следующие роли.

| Пользователь | Роль                  | Права | Для чего                   |
| ------------ | --------------------- | ----- | -------------------------- |
| `root`       | Суперпользователь     | Все   | Только для системных задач |
| `s3d0y`      | Основной пользователь | sudo  | Повседневная работа        |
| `hmsrvadm`   | Администратор         | sudo  | Управление сервером        |
Заведем пользователя `hmsrvadm`
### Создание пользователя 
#### **Интерактивное**  

автоматизирован, задает вопросы при создании пользователя. В основном Debian/Ubuntu, удобно и относительно безопастно завести пользователя. 

```sh
sudo adduser hmsrvadm
```

```
Особенности:

- Автоматически создает:
	- Домашнюю директорию (`/home/username/`)
	- Группу с именем пользователя
	- Настраивает права доступа для домашней директории 
	- Попросит установить пароль
- Запрашивает дополнительную информацию: ФИО, телефон и т.д.
```
#### **Неинтерактивное**  

все параметры передаются аргументами командной строки. Больше контроля над каждым параметром.   Есть во всех дистрибутивах Linux. Используюется в скриптах.

```
По умолчанию НЕ создает:

- Домашнюю директорию (без флага `-m`)
- Группу пользователя (без флага `-U`)
- Не устанавливает пароль по умолчанию (нужно отдельно `passwd`)

```

Например:

```sh
# Базовое создание пользователя (без домашней директории):
sudo useradd hmsrvadm

# Создание с домашней директорией и группой:
sudo useradd -m -U hmsrvadm

# Создание с полным контролем параметров:
sudo useradd -m -U -c "Home Server Admin" -s /bin/bash hmsrvadm

# не забыть поставить пароль
sudo passwd myadmin
```

### Основные операции на пользователями 

``` sh

# Посмотреть всех пользователей
cat /etc/passwd 

# Или так 
getent passwd 

# Проверим группы пользователя
groups hmsrvadm

# или
id hmsrvadm
```

Пример вывода "обычных" пользователей. Они больше "1000"
```sh 
clear
echo "=== Обычные пользователи системы ==="
getent passwd | awk -F: '$3 >= 1000 && $6 ~ /^\/home\// {
    print "👤 " $1 " (UID:" $3 ")" 
}' | sort
```

```sh
Перекоючиться на другого пользователя
sudo su - hmsrvadm 
```